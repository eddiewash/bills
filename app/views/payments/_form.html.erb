<%= simple_form_for [:invoice, @payment], html: {class: "form-horizontal"} do |f| %>
  <%= f.error_notification %>
  
  <%= f.input :payment_date, :as => "string" %>
  <%= f.input :amount %>
  <%= f.input :note, :input_html => { :class => 'span7', :rows => 3 } %>


  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>